# Biogas-Time-Series-ML-Engine
An App for forecasting biogas production using an XGBoost regression model. It processes historical plant data, applies feature engineering, supports exogenous driver overrides, offers interactive hyperparameter tuning, visualizes 24–72 hour forecasts with confidence bands, and lets users download results.
